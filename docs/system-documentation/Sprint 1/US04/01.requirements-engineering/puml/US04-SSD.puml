@startuml
skinparam monochrome true
skinparam packageStyle rectangle
skinparam shadowing false

autonumber

actor "Editor" as Editor
participant ":System" as System

activate Editor

    Editor -> System : asks to create a scenario
    activate System

        System --> Editor : shows a list of available maps and asks to select one
    deactivate System

    Editor -> System : selects a map
    activate System

        System --> Editor : requests name, start and end date
    deactivate System

    Editor -> System : types name, start and end date
    activate System

        System --> Editor : shows a list of locomotive types and asks to select the available ones
    deactivate System

    Editor -> System : selects the available locomotive types
    activate System

    loop while port exist
            System --> Editor : shows a list of cargoes and asks to select the ones the port import
        deactivate System

        Editor -> System : selects the cargoes the port import
        activate System

            System --> Editor : shows a list of cargoes and asks to select the ones the port export
        deactivate System

            Editor -> System : selects the cargoes the port export
            activate System

            System --> Editor : shows a list of cargoes and asks to select the ones the port transform
        deactivate System

            Editor -> System : selects the cargoes the port transform
            activate System
    end

        System --> Editor : requests the historical events
    deactivate System

    Editor -> System : types the historical events
    activate System

        System --> Editor : shows a list of industries and asks to select the ones available
    deactivate System

    Editor -> System : selects the available industries
    activate System

    loop while available generating industry exist
            System --> Editor : requests the generating industry factor
        deactivate System

        Editor -> System : types the generating industry factor
        activate System
    end

        System --> Editor : shows all data and requests confirmation
    deactivate System

    Editor -> System : confirms data
    activate System

        System --> Editor : displays operation success
    deactivate System

deactivate Editor
@enduml